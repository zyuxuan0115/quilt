version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  set-profiles:
    lang: rust
    handler: ./set-profiles
    image: zyuxuan0115/hr-set-profiles:latest
    secrets:
    - redis-password
    - mongodb-password
    - ingress-enable
