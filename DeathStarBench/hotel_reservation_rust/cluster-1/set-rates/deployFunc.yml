version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  set-rates:
    lang: rust
    handler: ./set-rates
    image: zyuxuan0115/hr-set-rates:latest
    secrets:
    - redis-password
    - mongodb-password
    - ingress-enable
