version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8081
functions:
  set-reservation:
    lang: rust
    handler: ./set-reservation
    image: zyuxuan0115/hr-set-reservation:latest
    secrets:
    - redis-password
    - mongodb-password
    - ingress-enable
