version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  upload-rating:
    lang: rust
    handler: ./upload-rating
    image: zyuxuan0115/mm-upload-rating:latest
    secrets:
    - redis-password
    - mongodb-password
    - ingress-enable
