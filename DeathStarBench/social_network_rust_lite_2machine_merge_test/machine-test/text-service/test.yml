version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  text-service-new:
    lang: rust
    handler: ./text-service-new
    image: zyuxuan0115/deathstarbench-test-t-new-merged:latest
    secrets:
    - ipv4-addr
    - redis-password
